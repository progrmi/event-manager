<!DOCTYPE html>
<html lang="en">
  <%- include('partials/head') %>
  <body>
    <!-- prettier-ignore -->
    <%- include('partials/header', { pageClass: 'Organiser', headerTitle: '🎫 Event Manager - Register', headerSubtitle: 'Your Complete Event Management Solution' }) %>

    <div class="register-container">
      <div class="register-header">
        <h1>🎫 Event Manager</h1>
        <p>Create your account</p>
      </div>

      <% if (typeof message !== 'undefined' && message) { %>
      <div class="alert alert-<%= message.type %>"><%= message.text %></div>
      <% } %>

      <div class="role-info">
        <h4>🎭 Account Types</h4>
        <p>
          <strong>Organiser:</strong> Create, manage, and view bookings for
          events
        </p>
        <p><strong>Attendee:</strong> Browse and book available events</p>
      </div>

      <form action="/auth/register" method="POST" class="register-form">
        <div class="form-group">
          <label for="name">👤 Full Name</label>
          <input
            type="text"
            id="name"
            name="name"
            required
            placeholder="Enter your full name"
            value="<%= typeof formData !== 'undefined' ? formData.name || '' : '' %>"
          />
        </div>

        <div class="form-group">
          <label for="email">📧 Email Address</label>
          <input
            type="email"
            id="email"
            name="email"
            required
            placeholder="Enter your email"
            value="<%= typeof formData !== 'undefined' ? formData.email || '' : '' %>"
          />
        </div>

        <div class="form-group">
          <label for="password">🔒 Password</label>
          <input
            type="password"
            id="password"
            name="password"
            required
            placeholder="Create a password"
            minlength="6"
          />
        </div>

        <div class="form-group">
          <label for="confirmPassword">🔒 Confirm Password</label>
          <input
            type="password"
            id="confirmPassword"
            name="confirmPassword"
            required
            placeholder="Confirm your password"
            minlength="6"
          />
        </div>

        <button type="submit" class="btn-register">🚀 Create Account</button>
      </form>

      <div class="login-link">
        <p>Already have an account? <a href="/auth/login">Sign in here</a></p>
      </div>
    </div>
    <%- include('partials/footer') %> <%- include('partials/scripts') %>
  </body>
</html>
